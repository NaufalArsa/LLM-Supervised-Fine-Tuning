#  Scripts


Folder ini berisi script Python untuk menjalankan pipeline data.


- `clean_data.py` → script pembersihan data mentah.
- `preprocess.py` → script preprocessing (format schema JSON).
- `split_dataset.py` → script pembagian data train/val/test.


Semua script harus bisa dijalankan secara modular.
